apiVersion: v1
data: {}
kind: Secret
metadata:
  labels:
    k8s-app: thanos-ruler
  name: thanos-ruler-kube-rbac-proxy
  namespace: openshift-user-workload-monitoring
stringData:
  config.yaml: |-
    "authorization":
      "resourceAttributes":
        "apiVersion": "monitoring.coreos.com/v1"
        "namespace": "{{ .Value }}"
        "resource": "prometheusrules"
      "rewrites":
        "byQueryParameter":
          "name": "namespace"
type: Opaque
